---
description: Archive a URL (Article or YouTube) into the Library
---

# Archive URL

1. Archive the URL

   ```bash
   python3 scripts/librarian/archive.py "{{url}}"
   ```

2. Confirm
   - Check `.context/library/` for the new file.
